$(document).ready(function () {

    $(function () {
        $("div.lazy, a.lazy").lazyload({
            effect: "fadeIn"
        });
    });

    //var validator = $('#registrationForm').validator();
    //
    //validator.on('validate.bs.validator', function() {
    //    console.log(arguments);
    //});
});